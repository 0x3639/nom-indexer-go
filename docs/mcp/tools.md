# MCP tools

The MCP server has not been implemented yet, so no tools are exposed by
the current binary.

The planned shape is read-only, parameterized queries over the indexer
schema: examples include rewards by address, pillar lookup by name, and
bridge requests by token. When the server exists, this page should list
each tool name, input schema, backing SQL or repository method, and
example result.
