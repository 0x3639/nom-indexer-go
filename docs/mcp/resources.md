# MCP resources

The MCP server has not been implemented yet, so no resources are exposed
by the current binary.

The planned shape is read-only resources backed by the same Postgres
tables documented in [`docs/schema/`](../schema/index.md). When the
server exists, this page should list each resource URI, its backing
table or query, parameters, and returned fields.
