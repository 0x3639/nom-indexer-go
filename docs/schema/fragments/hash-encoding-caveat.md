!!! note "Hash encoding"
    Hashes are stored as **64-character lowercase hexadecimal strings** in
    `TEXT` (no `0x` prefix, no leading whitespace). Addresses are stored as
    bech32-encoded `z1…` strings. Token standards are stored as
    `zts1…` strings. Full discussion in `schema/conventions.md` under
    "Hash encoding".
